[package]
name = "stateviz-chaosbf_plugin"
version = "0.1.0"
edition = "2021"

[dependencies]
stateviz-manifold = { path = "../../manifold" }
stateviz-scene = { path = "../../scene" }
thiserror = "1"
serde = { version = "1", features = ["derive"] }
