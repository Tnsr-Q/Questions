---
# DEPRECATED: mutation-firewalld
# This manifest is retained for historical reference only.
# The dual-server architecture was evaluated and explicitly deprecated in favor of
# a monolithic Tier-1 ConnectRPC switchboard (gatewayd) to optimize for absolute
# minimum latency between Swarm consensus and WebGL rendering.
#
# The admission pipeline has been condensed from a multi-stage microservice into
# a strict, synchronous validation block within UpdateAlphaRoute in gatewayd.
#
# See: docs/architecture/20-route-mutation-firewall.md (DEPRECATED)
# See: cmd/gatewayd/main.go (Unified ConnectRPC Monolith)
---
# apiVersion: apps/v1
# kind: Deployment
# metadata:
#   name: mutation-firewalld
#   namespace: darwins-gate
#   labels:
#     app: mutation-firewalld
#     component: controlplane
#     managed-by: kustomize
#     deprecated: "true"
#     deprecation-reason: "Replaced by unified gatewayd monolith"
# spec:
#   # ... [original spec retained for reference only]
